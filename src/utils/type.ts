type technologies = {
  name: string;
};
export type project = {
  title: string;
  description: string;
  visit: string;
  github: string;
  img: string;
  technologies: technologies[];
};

export type typeProjects = project[];
